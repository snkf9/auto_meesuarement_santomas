"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const EventEmitter = require("events");
const socket_1 = require("./socket");
test('should create an socket module instance', () => {
    const uw = new (class Test {
    })();
    const privateSocketTokenRef = {};
    const socket = new socket_1.default(uw, privateSocketTokenRef);
    expect(socket).toBeInstanceOf(socket_1.default);
    expect(socket['emitter']).toBeInstanceOf(EventEmitter);
    expect(socket['uw']).toEqual(uw);
    expect(socket).toMatchInlineSnapshot(`
Socket {
  "emitter": EventEmitter {
    "_events": Object {},
    "_eventsCount": 0,
    "_maxListeners": undefined,
    Symbol(kCapture): false,
  },
  "uw": Test {},
}
`);
});
//# sourceMappingURL=socket.spec.js.map