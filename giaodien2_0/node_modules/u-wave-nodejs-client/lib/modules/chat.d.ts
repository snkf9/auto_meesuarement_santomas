import { uWave } from '..';
export default class Chat {
    private uw;
    constructor(uw: uWave);
    sendChat(message: string): void;
    deleteAll(): Promise<null>;
    deleteAllByUser(userId: string): Promise<null>;
    deleteMessage(messageId: string): Promise<null>;
    muteUser(userId: string, duration: number): Promise<null>;
    unmuteUser(userId: string): Promise<null>;
}
